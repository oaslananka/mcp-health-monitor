# Optional path override for the local SQLite database.
HEALTH_MONITOR_DB=

# Set to 1 to enable the opt-in background scheduler.
HEALTH_MONITOR_AUTO_CHECK=0

# Port for the HTTP entrypoint (`npm run start:http`).
PORT=3000
